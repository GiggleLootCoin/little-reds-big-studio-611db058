# Deployment truth

A merged commit is not the same as a deployed commit. A green CI run is not the same as live inference. Production status must be reported separately for source validation, deployment, and real provider execution.
