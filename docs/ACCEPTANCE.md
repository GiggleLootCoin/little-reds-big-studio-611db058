# Acceptance

A build is ready to merge when CI is green. A generation feature is ready to call working only after a real browser request returns a usable artifact. External provider availability cannot be inferred from source code alone.
