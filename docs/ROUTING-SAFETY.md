# Routing safety

Provider selection is best-effort. The router never exposes credentials to public Spaces, never treats a queued job as success, and never loops indefinitely over one failing provider.
