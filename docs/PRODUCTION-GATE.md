# Production gate

Do not call the Studio fully working merely because CI passes. CI proves source validity and buildability. Production acceptance additionally requires real browser execution and a usable artifact for each enabled generator category.
