# Provider selection

Selection is dynamic. Capability, current API schema, runtime health, and local cooldown all affect selection. A provider with an exhausted free quota is skipped rather than retried repeatedly.
