# Core guarantees

The core Studio does not require a paid API key. Provider failures do not silently become success. User inputs remain available after failed generation. The application is Android-first and browser-friendly. Public AI services are treated as replaceable workers.
