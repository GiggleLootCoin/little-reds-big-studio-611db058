# Core cost policy

The core Studio must not require a paid API, subscription, or developer key. Public/free providers are interchangeable fallbacks. Optional user-supplied services may exist separately, but the Studio must remain usable without them.
