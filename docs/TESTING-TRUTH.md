# Testing truth

Build validation and live inference validation are separate. The project may only report a generator as working after a real browser request returns a usable artifact. CI success alone does not prove that an external public AI provider is currently available.
