# Final architecture

Android/browser client first, free provider router second, public AI workers as replaceable backends, and real-artifact validation at the boundary. No single provider is a hard dependency and no paid API key is required for the core path.
