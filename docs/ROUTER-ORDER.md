# Router order

Prefer local Android/browser execution when a task can be completed locally. Otherwise prefer a healthy compatible free public worker. Never wait on an exhausted worker when another compatible worker is available.
