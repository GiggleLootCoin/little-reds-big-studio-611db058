# Cost

The core application is designed to remain usable without a paid service. External free-provider quotas can vary; the router handles that variability instead of promising unlimited third-party compute.
