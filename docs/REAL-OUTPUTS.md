# Real outputs

Generation success is defined by an actual usable artifact. Text-only success, a provider HTTP 200, a queued job, or a UI animation is not sufficient. The UI must keep inputs available when a provider fails and must expose a clear failure when no free compatible engine is available.
