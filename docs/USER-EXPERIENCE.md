# Failure UX

When a provider fails, preserve all user inputs, show that the current engine failed, and continue to the next compatible free engine. When no engine remains, show a clear retryable message rather than an indefinite spinner or false success.
