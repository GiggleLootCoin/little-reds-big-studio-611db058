# Android failsafe

Browser/device capability failures must fall back gracefully. Microphone and local AI are optional enhancements; remote free providers remain available when the device cannot run a local model.
