# Generation truth

The UI must distinguish connecting, queued, generating, succeeded, and failed. Succeeded requires a real artifact or completed response. Quota errors are failed-provider states and trigger failover.
