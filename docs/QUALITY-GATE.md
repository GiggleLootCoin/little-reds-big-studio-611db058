# Quality gate

Before a change is merged, CI must pass install, formatting, type-check, lint, and production build. After deployment, external AI generation remains a separate runtime verification step because provider availability is outside source-control CI.
