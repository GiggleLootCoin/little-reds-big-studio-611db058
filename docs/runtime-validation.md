# Critical runtime validation

This release validates the mobile Buddy conversation path and the bundled Gradio generation transport together. The production gates are TypeScript, formatting, lint, and the production build before deployment.
