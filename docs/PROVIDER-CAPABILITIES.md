# Provider capabilities

The provider registry is the source of capability routing. A worker is selected only if it advertises the requested capability and its live Gradio API exposes a compatible endpoint.
