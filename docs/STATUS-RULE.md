# Status rule

Never display a success state until the runtime has a completed Buddy response or a usable artifact URL/blob. Provider connection, queue acceptance, or HTTP 200 is only an intermediate state.
