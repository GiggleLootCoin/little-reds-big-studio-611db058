# Buddy / studio visual references

Place the two approved binary reference images here when the GitHub connection supports binary uploads:

- `buddy-character-reference.jpg`
- `studio-reference.jpg`

These files are visual references only. They must never become API credentials or be uploaded to a paid inference provider by default.
