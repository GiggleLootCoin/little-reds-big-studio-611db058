# Finish line

The code finish line is a green CI build and merged branch. The product finish line is separate: each enabled live generator must be exercised from the deployed Android browser and return a real artifact. External provider availability cannot be honestly certified from GitHub source alone.
