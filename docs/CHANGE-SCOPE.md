# Change scope

This branch contains runtime resilience only. It does not introduce paid providers, credentials, or a new hosting dependency. It keeps the existing free-first architecture while preventing exhausted providers from repeatedly blocking generation.
