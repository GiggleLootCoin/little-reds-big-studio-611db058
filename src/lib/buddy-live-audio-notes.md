Buddy hands-free audio uses MediaRecorder rather than browser SpeechRecognition so the microphone selected by the user is actually honored. It captures until speech has ended naturally, sends the recording to real speech-to-text, sends the transcript to the real Buddy AI path, and speaks the response before listening again. No fixed conversation-turn limit is imposed by the Studio.