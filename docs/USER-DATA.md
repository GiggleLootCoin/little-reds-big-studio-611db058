# User data

The implementation should not claim literal unlimited storage or absolute permanence unless the deployed storage service provides it. The UI should communicate persistence accurately and keep deletion explicit.
