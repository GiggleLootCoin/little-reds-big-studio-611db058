# Final user path

The user enters a prompt, the Studio selects a healthy compatible free engine, performs live API discovery, runs the job, validates the returned artifact, and presents the result. If that engine fails, the router continues automatically.
